## What changed

<!-- One or two sentences: what does this PR do and why? -->

## Checklist

- [ ] `npm run lint` passes with zero errors
- [ ] `npm test` passes (all vitest suites green)
- [ ] `npm run build` succeeds (`VITE_BASE=/hrms/` for the Pages build)
- [ ] Manual check done in `npm run dev` (pages touched by this change open and behave)
- [ ] No new `localStorage` keys or tenant-scoping regressions (multi-tenant rules respected)

## Notes for reviewers

<!-- Screenshots, edge cases, follow-ups — anything that helps the review. -->
