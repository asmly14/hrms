/**
 * Contracts module manifest (read by the Stage-5 integration agent).
 */
export { default as ContractsPage } from './ContractsPage';

export const routes = [{ path: '/contracts', title: 'Contracts' }];
