/**
 * Onboarding module manifest (read by the Stage-5 integration agent).
 */
export { default as OnboardingPage } from './OnboardingPage';

export const routes = [{ path: '/onboarding', title: 'Onboarding' }];
